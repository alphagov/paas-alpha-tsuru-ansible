---
  - include: firewall_pre.yml
  - name: allow connection to Redis
    ufw: rule=allow port={{ redis_port }} proto=tcp src={{ hostvars[item][ip_field_name] }}
    with_items:
      - "{{ groups[tsuru_api_host_name] }}"
      - "{{ groups[tsuru_router_host_name] }}"
