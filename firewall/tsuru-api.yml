---
  - include: firewall_pre.yml
  - name: reject everything and enable UFW
    ufw: state=enabled policy=allow
#  - name: allow connection to Tsuru API
#    ufw: rule=allow port={{ item }} proto=tcp
    with_items:
    - 80
    - 443
